<div class="container content">
    <div class="row">
        <div class="col-sm-10 col-sm-offset-1">
            <div class="row">
                <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1 welcome-targetpage">
                    <h3 class="text-center">You can now use Contetto!</h3>
                    <div class="box">
                        <form class="form-horizontal" {{action "createTargetAudience" on="submit"}} autocomplete="off">
                            <div class="row">
                                <div class="col-xs-12 text-center">
                                    <p>But we recommend setting up our Smart Features...</p>
                                    <p>Contetto can recommend when to post, and find great engaging content for your target audience - we just need a little more information about your brand.</p>
                                    <button class="btn btn-success text-uppercase" type="button" {{action 'transitionTo' 'welcome.schedule'}}>Skip this step</button>
                                    <h5>Define your target audience for <b>{{model.audience.brand.name}}</b></h5>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-xs-12 col-sm-12 text-center"><h5><b>Where is your Target Audience?</b></h5></div>
                                <div class="col-xs-12 col-sm-12">
                                    {{#x-select multiple=true class="custom_dropdown form-control" action="selectLocation"}}
                                        {{#each model.locations as |location|}}
                                            {{#x-option value=location.id}}{{location.name}}{{/x-option}}
                                        {{/each}}
                                    {{/x-select}}
                                </div>
                            </div>
                            <div class="row" style="padding-top: 15px;">
                                <div class="col-xs-12 col-sm-12 text-center"><h5><b>What Age Ranges Apply?</b></h5></div>
                                <div class="col-xs-12 col-sm-12">
                                    {{#x-select multiple=true class="custom_dropdown form-control" action="selectAgeRange"}}
                                        {{#each model.ageRanges as |age|}}
                                            {{#x-option value=age.id}}{{age.name}}{{/x-option}}
                                        {{/each}}
                                    {{/x-select}}
                                </div>
                            </div>
                            <div class="row" style="padding-top: 15px;">
                                <div class="col-xs-12 col-sm-12 text-center"><h5><b>What Genders Apply?</b></h5></div>
                                <div class="col-xs-12 col-sm-12">
                                    {{#x-select multiple=true class="custom_dropdown form-control" action="selectGender"}}
                                        {{#each model.gender as |gender|}}
                                            {{#x-option value=gender.id}}{{gender.name}}{{/x-option}}
                                        {{/each}}
                                    {{/x-select}}
                                </div>
                            </div>
                            <div class="row text-right" style="padding: 15px 0 0;">
                                <div class="col-xs-12">
                                    <button class="btn btn-success text-uppercase" type="submit">Next</button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
